version: 2

models:
  - name: dim_employees_territories
    columns:
      - name: employee_sk
        description: The primary key of the dim_employees_territories
        tests:
          - unique
          - not_null